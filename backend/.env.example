# Backend Environment Variables

# Database Connection
DATABASE_URL=postgresql://traveler:travel123@postgres:5432/travel_map_db

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration - Change this to your frontend URL in production
CORS_ORIGIN=http://localhost:3000

# Mapbox Token (if using Mapbox - currently using OpenStreetMap which doesn't require a token)
# MAPBOX_TOKEN=your_mapbox_token_here
